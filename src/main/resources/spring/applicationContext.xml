<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
			http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
			http://www.springframework.org/schema/context
			http://www.springframework.org/schema/context/spring-context-4.0.xsd"
	   default-lazy-init="false">

	<!-- 注解扫描 -->
	<context:component-scan base-package="com.java" />

	<!-- 使用spring自带的占位符替换功能 -->
	<bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<!-- 允许JVM参数覆盖 -->
		<property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE" />
		<!-- 忽略没有找到的资源文件 -->
		<property name="ignoreResourceNotFound" value="true" />
        <!-- 编码 -->
        <property name="fileEncoding" value="utf-8"/>
		<!-- 配置资源文件 -->
		<property name="locations">
			<list>
				<value>classpath:config.properties</value>
				<value>classpath:log4j.properties</value>
				<!--<value>classpath:config.properties</value>-->
				<!--<value>classpath:redis.properties</value>-->
			</list>
		</property>
	</bean>


	<!-- 导入其他Spring管理的资源 -->
	<import resource="classpath:spring-httpclient.xml"/>
    <import resource="classpath:jms/applicationContext-jms-base.xml"/>
    <import resource="classpath:jms/applicationContext-jms-sender.xml"/>
    <import resource="classpath:jms/applicationContext-jms-listener.xml"/>

</beans>